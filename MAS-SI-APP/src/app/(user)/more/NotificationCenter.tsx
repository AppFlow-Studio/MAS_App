// Re-export NotificationEvents so it can be accessed within the "more" tab stack.
// This prevents a tab switch to myPrograms when navigating from the more screen.
export { default } from '../myPrograms/notifications/NotificationEvents';
